import 'package:flutter/material.dart';

class Disease4 extends StatelessWidget {
  const Disease4({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
