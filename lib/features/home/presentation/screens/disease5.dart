import 'package:flutter/material.dart';

class Disease5 extends StatelessWidget {
  const Disease5({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
