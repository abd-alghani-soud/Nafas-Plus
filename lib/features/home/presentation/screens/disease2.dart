import 'package:flutter/material.dart';

class Disease2 extends StatelessWidget {
  const Disease2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
