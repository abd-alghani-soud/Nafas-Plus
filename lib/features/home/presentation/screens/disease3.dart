import 'package:flutter/material.dart';

class Disease3 extends StatelessWidget {
  const Disease3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
